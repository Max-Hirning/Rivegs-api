export enum UserErrorMessages {
  findOne = 'No such user',
}

export enum UserSuccessMessages {
  removeOne = 'Profile was removed',
  updateProfile = 'Changes were saved',
  updatePassword = 'Password was changed',
}